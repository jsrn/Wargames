#include <stdio.h>

int main( int argc, char *argv[] )
{
	puts("Hello world.");

	puts("more");
	puts("complex");
	puts("than");
	puts("hello");
	puts("world");

	// -- Extra Credit --
	// 1) Create an all: ex1 target that will build ex1 with just the command
	// make.
	// 2) Read man make to find out more information on how to run it.
	// 3) Read man cc to find out more information on what the flags -Wall and
	// -g do.
	// 4) Research Makefiles online and see if you can improve this one even
	// more.
	// 5) Find a Makefile in another C project and try to understand what it's
	// doing.

	return 0;
}